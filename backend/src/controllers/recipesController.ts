import express from 'express';

const indexRecipes = (
  req: express.Request,
  res: express.Response,
  next: express.NextFunction,
) => {

};

const getRecipe = (
  req: express.Request,
  res: express.Response,
  next: express.NextFunction,
) => {
};

const createRecipe = (
  req: express.Request,
  res: express.Response,
  next: express.NextFunction,
) => {
};

const updateRecipe = (
  req: express.Request,
  res: express.Response,
  next: express.NextFunction,
) => {
};

export {
  indexRecipes,
  createRecipe,
  getRecipe,
  updateRecipe,
};
